mcdp {
    provides applied_load [N/mm]
    provides element_length [mm]
    requires moment_demand [N*mm]
    requires shear_demand [N]

    required moment_demand >= provided applied_load * provided element_length^2 / 8
    required shear_demand >= provided applied_load * provided element_length / 2
}

